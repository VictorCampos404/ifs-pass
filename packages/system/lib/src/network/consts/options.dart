const Duration REQUEST_TIMEOUT = Duration(milliseconds: 20000);
const Duration RECIVE_TIMEOUT = Duration(milliseconds: 20000);
const String BASEURL = '';
const String CONTENTTYPE = "application/json";
