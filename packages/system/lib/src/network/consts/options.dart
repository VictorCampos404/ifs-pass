const Duration REQUEST_TIMEOUT = Duration(milliseconds: 40000);
const Duration RECIVE_TIMEOUT = Duration(milliseconds: 40000);
const String BASEURL = 'https://us-central1-ifs-pass-hml.cloudfunctions.net';
const String CONTENTTYPE = "application/json";
